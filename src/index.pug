doctype html
html(lang="en/us")  
    head
        title Church
        link(href='style.css', rel='stylesheet',type="text/css")
    body(class="_body")
        main
            nav
                a(href="#groundfloor", onclick="focusOnLevel('level0')") ground floor
            div(id='canvas')
        script(src="https://cdnjs.cloudflare.com/ajax/libs/gsap/latest/TweenLite.min.js")
        script(src='https://cdnjs.cloudflare.com/ajax/libs/three.js/r83/three.min.js')
        script(src='script.js')
        script.
            window.addEventListener('load', init, false);
            function init() {
                initScene();
                church = new Building();
                scene.add(church.mesh)

                render();
            }
        
            
            